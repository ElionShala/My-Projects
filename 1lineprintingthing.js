const TheCharacterToPrint='*';const NumberOfTimes=10;function printIt(){const charLength=TheCharacterToPrint.length;const maxCharWidth=charLength*(2*NumberOfTimes-1);for(let i=1;i<NumberOfTimes;i++){const totalWidth=charLength*(2*i-1);const spacesToPrint=Math.floor((maxCharWidth-totalWidth)/2);console.log(' '.repeat(spacesToPrint)+TheCharacterToPrint.repeat(2*i-1))}for(let i=NumberOfTimes;i>0;i--){const totalStarsWidth=charLength*(2*i-1);const spacesToPrint=Math.floor((maxCharWidth-totalStarsWidth)/2);console.log(' '.repeat(spacesToPrint)+TheCharacterToPrint.repeat(2*i-1))}}printIt();
